# Session signing secret. Generate with: openssl rand -base64 48
AUTH_SECRET=

# Password for the single local operator account.
# Change this before exposing the app to anything but localhost.
APP_PASSWORD=changeme

# Where the SQLite database lives. Defaults to ./data
DATA_DIR=./data

# Optional: an OpenAI-compatible endpoint for the AI Copilot narrative.
# Leave unset and the Copilot reports itself unavailable rather than inventing text.
COPILOT_BASE_URL=
COPILOT_API_KEY=
COPILOT_MODEL=
